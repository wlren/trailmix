import 'package:flutter/material.dart';

class DND {
  final String title;
  final String description;
  final Icon icons;

  const DND({
    @required this.title,
    @required this.description,
    @required this.icons,
  });
}
